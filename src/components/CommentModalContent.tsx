import React from 'react';

/**
 * Componente placeholder para el contenido del modal de comentarios.
 * La lógica principal de comentarios es manejada por GiscusComments.tsx.
 * Este archivo fue corregido porque contenía un script HTML inválido.
 */
const CommentModalContent: React.FC = () => {
    return (
        <div>
            {/* Este componente actualmente no se usa. La lógica de comentarios está en GiscusComments. */}
        </div>
    );
};

export default CommentModalContent;
